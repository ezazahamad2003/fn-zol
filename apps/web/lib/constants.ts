// Client-safe constants (no server-only imports). Import these from client
// components instead of reaching into modules that pull in next/headers.

export const ACTIVE_TENANT_COOKIE = "active_tenant";
